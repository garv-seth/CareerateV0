import express from 'express';
const router = express.Router();

// Placeholder routes for shareCard
router.get('/', (req, res) => res.json({ message: 'Share Card route placeholder' }));

export default router; 