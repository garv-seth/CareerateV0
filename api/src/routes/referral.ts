import express from 'express';
const router = express.Router();

// Placeholder routes for referral
router.get('/', (req, res) => res.json({ message: 'Referral route placeholder' }));

export default router; 