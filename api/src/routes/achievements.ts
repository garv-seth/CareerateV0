import express from 'express';
const router = express.Router();

// Placeholder routes for achievements
router.get('/', (req, res) => res.json({ message: 'Achievements route placeholder' }));

export default router; 