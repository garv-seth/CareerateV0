import express from 'express';
const router = express.Router();

// Placeholder routes for resume
router.get('/', (req, res) => res.json({ message: 'Resume route placeholder' }));

export default router; 